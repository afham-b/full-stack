const math = require('./math')//require the math.js file
console.log(math); //print an array of math objects
const square = math.sqr(9); 
console.log(square); 

//const {PI, sqr} = require('./math') would only bring the 
// pi const and sqr functino over

 